this example APM creates a UI group in the sidebar that has an output text area, input text box, and button with text "Echo".

when the "Echo" button is clicked, the text in the output area is set to the value of the input box using a command.

(NOTE: this could be achieved without a command, but is done with a command to provide a full example)

